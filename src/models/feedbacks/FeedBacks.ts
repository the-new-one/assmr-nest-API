export interface UserFeedBacksModel {
  userId: number;
  fullName: string;
  email: string;
  feedComments: string;
  satisfaction: string;
} // use this when a user want to send a FeedBack to system;
